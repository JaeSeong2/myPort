// MediaPipe Pose 더미 stub — 이 앱은 MoveNet(tfjs) 런타임만 사용하므로 @mediapipe/pose는 불필요.
// pose-detection esm이 정적으로 import 하는 `Pose`만 존재하면 되고, 런타임에선 MoveNet 경로만 실행되어 미사용. - 2026-08-31
export class Pose {}
export default { Pose }
